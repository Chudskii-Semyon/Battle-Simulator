import { mockSquadRepository } from './squad-repository.mock';
import { mockSoldierRepository } from './soldier-repository.mock';
import { mockVehicleRepository } from './vehicle-repository.mock';
import { mockOperatorRepository } from './operator-repository.mock';
import { mockConfigRepository } from './config-repository.mock';
import { mockArmyRepository } from './army-repository.mock';
import { mockUserRepository } from './user-repository.mock';

export {
  mockArmyRepository,
  mockSquadRepository,
  mockSoldierRepository,
  mockVehicleRepository,
  mockOperatorRepository,
  mockConfigRepository,
  mockUserRepository,
};
